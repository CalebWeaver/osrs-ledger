# Module: Core Domain

## Responsibility
Defining the shared language (Ubiquitous Language) and types used across the system.

## Components
- **`src/lib/types.ts`**: TypeScript definitions.
  - `Method`, `Skill`, `ItemAmount`, `PriceMap`, `MethodEvaluation`.

## Current State
- ✅ Strongly typed interfaces for all major entities.
- ✅ Centralized location for domain concepts.
